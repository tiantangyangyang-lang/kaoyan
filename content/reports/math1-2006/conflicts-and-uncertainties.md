# Math1 2006 — Conflicts and Uncertainties (MD-Finalized)

**运行ID**: 20260620-164031-cc-math1-md-finalize-year-2006
**生成日期**: 2026-06-20

---

## 冲突统计（MD-Finalization 后）

| 类别 | 修复前数量 | 修复后数量 |
|------|-----------|-----------|
| Paper ↔ Solution 公式冲突 | 1 | 0（已确定性修复） |
| 选项标签缺失（源 OCR 问题） | 6（4题） | 0（已确定性修复） |
| 节标题污染 | 4处 | 0（已清理） |
| OCR 换行断词 | 1处 | 0（已修复） |
| 解析文本笔误 | 1处 | 0（已修复） |
| 非标准 LaTeX 记法 | 1处 | 1（保留，info级） |

---

## 已解决的冲突

### 1. Q16 - 公式冲突：`x^n` vs `x_n^2` ✓

**决策**: 采纳 solution Markdown 的 `x_n^2`。

**证据链**:
- Paper Markdown line 107: `\frac{1}{x^n}`
- Solution Markdown line 262: `\frac{1}{x_n^2}`
- 数学推导一致性: `(sin x_n/x_n)^{1/x_n^2} = e^{(sin x_n - x_n)/x_n^3}` 要求指数为 `1/x_n^2`
- OCR 错误模式: `x_n^2` → `x^n` (下标 n 丢失，上标 2 被识别为 n)

### 2. Q08/Q13/Q14 - 选项标签 ✓

通过源 Markdown 行序列和内容检查恢复所有缺失的选项标签 (A)~(D)。

### 3. Q06/Q14 - 节标题污染 ✓

从 stem 和 explanation 中移除误入的 `# 二、选择题` 和 `# 三、解答题` 节标题。

### 4. Q18 - 笔误 ✓

`代人` → `代入`

### 5. Q22 - OCR 断词 ✓

`二维随机变量\n\n量` → `二维随机变量`

---

## 仍需人工确认的 items（非阻断）

1. **Q08 答案正确性核对**: 虽然 solution 给出答案 (C)，且区域转换从极坐标到 Y 型的推导正确，但人工需确认 (A)~(D) 四个选项在 PDF 中确实按此顺序排列。

2. **Q21 非标准 LaTeX `\binom` 记法**: 解析中使用 `\binom{-1}{2}` 表示 3 维列向量的前两个分量是 `\binom` 的标准用法（二项式系数），此处用来表示列向量不标准但可读。保留原始记法。

3. **Q23 似然函数构造**: 题目密度函数在 [0,1) 取 θ、[1,2) 取 1-θ、其他为 0。似然函数 L(θ)=θ^N·(1-θ)^{n-N} 的构造基于每个样本值落入各区间的计数——人工需确认这种分段常数密度的似然函数构造方式。

---

## 无法执行的检查

| 检查项 | 状态 | 原因 |
|--------|------|------|
| PDF 视觉对照 | not_run | MD-first 任务不需要 PDF；所有修复来自 Markdown |
| 来源仓库完整性校验 | not_run | 仓库为 dirty 状态（之前批次残留） |
| 图片文件完整性 | not_run | 图片引用存在但未在此任务中验证哈希 |
