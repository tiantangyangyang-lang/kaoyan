执行 `真题解析首批任务.md` 中的 `PILOT-CC-M1-2020：数学一 2020 确定性转换`。

先阅读：
- `真题内容解析与代理处理规范.md`
- `真题解析首批任务.md`

只读使用数学一来源库。输出只能进入 `content/staging/math1/2020/`、`content/reports/pilot-math1-2020/` 以及完成任务所需的转换脚本和测试。

所有题目必须保持 `needs_human_review`。不要判断数学答案正确性，不要静默修复不明确公式，不要修改来源库。

执行优先级与实现边界：

1. 只创建一套 Python 标准库转换实现及其 `unittest` 测试，禁止创建 Node/JavaScript 或其他平行实现。
2. 转换脚本必须通过命令行参数接收输入快照/来源副本与输出目录，禁止硬编码某次运行的 Run ID 或 `source-mirror` 绝对路径。
3. 先交付 23 道候选题、异常清单、验证结果、`agent-result.json` 和 `agent-report.md`，再做非必要改进。
4. 若剩余轮次不足，停止扩展功能，立即运行现有检查并如实写入结果日志；不得用猜测内容补齐题目、答案或解析。
5. 2020 年应得到 8 道选择题、6 道填空题、9 道解答题；数量或切分不一致时必须标记异常并保持 `needs_human_review`，不得为了凑数静默修复。
6. 禁止修改 `task_plan.md`、`notes.md` 或任务允许写入目录之外的任何项目文件。结果日志中的 `changedFiles` / `createdFiles` 必须与实际项目变更一致，测试命令和测试数量必须如实报告。
